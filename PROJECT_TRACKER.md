# Sentinel Project Tracker

Reference spec: [sentinel-platform-spec.md](./sentinel-platform-spec.md)

This file tracks overall program progress for the Sentinel platform. Keep it stable, high-signal, and current. Use `CURRENT_CONTEXT.md` for the current working state, `TODO.md` for the immediate queue, and this file for phase-level status and major milestones.

## Status Legend

- `[done]` Completed and accepted
- `[in-progress]` Actively being built
- `[blocked]` Waiting on a decision or dependency
- `[not-started]` Not started yet

## Current Program State

- Current phase: `Phase 0 - foundation implementation underway`
- Current objective: establish a runnable Sentinel control plane with the chosen stack, turn the completed SBOM and breach-intel foundations into live integrations, and keep moving M1 toward a real deployment-backed repository scan path
- Tracking cadence: update this file whenever a milestone changes state

## Delivery Model

We will not build Sentinel as one large feature drop. We will build it in layers:

1. Platform foundation
2. Core data plane and orchestration
3. Phase 1 MVP capabilities
4. Operator-facing API and dashboard
5. Advanced intelligence and autonomy

## Workstreams

| ID | Workstream | Scope | Depends On | Status |
|---|---|---|---|---|
| WS-00 | Delivery setup | Tracking docs, repo conventions, milestone definitions, implementation split | None | `[done]` |
| WS-01 | Technical decisions | Monorepo shape, service boundaries, primary languages, deployment approach, local dev strategy | WS-00 | `[done]` |
| WS-02 | Repository bootstrap | App skeletons, package management, linting, formatting, CI, environment config | WS-01 | `[in-progress]` |
| WS-03 | Core platform services | API gateway, auth, tenant model, event router, workflow runner, agent task execution | WS-02 | `[in-progress]` |
| WS-04 | Data plane | PostgreSQL, pgvector, Redis, object storage, graph store abstraction, schema migrations | WS-02 | `[in-progress]` |
| WS-05 | Integration layer | GitHub, GitLab, CI providers, Slack, webhook ingest/emit | WS-03 | `[done]` — GitHub ✅, GitLab ✅, Bitbucket ✅, Slack ✅, GitHub Actions native plugin ✅, PagerDuty ✅, Jira ✅, Linear ✅, Azure DevOps ✅, CircleCI ✅, Buildkite ✅, Jenkins ✅ (spec §4.6.2 CI coverage fully closed) |
| WS-06 | SBOM registry MVP | Dependency ingestion, snapshot storage, diffing, export endpoints | WS-04, WS-05 | `[in-progress]` |
| WS-07 | Breach intel MVP | Feed ingestion, normalization, deduplication, impact fanout | WS-03, WS-04 | `[in-progress]` |
| WS-08 | Semantic fingerprinting MVP | Parsing, chunking, embeddings pipeline, vector search, candidate findings | WS-04 | `[in-progress]` |
| WS-09 | Exploit validation MVP | Sandbox job lifecycle, reproducible validation flow, artifact storage | WS-03, WS-04 | `[done]` — Phase 1 (real HTTP sandbox: HttpProbeModule, InjectionModule, AuthBypassModule, CVE patterns, classify_outcome(), PoC generator, 63 tests) ✅; Phase 2 (LLM injection: LlmInjectionModule, 8 payloads×6 paths×3 formats=144 attempts, canary-based detection, 33 tests) ✅; spec §3.9/§3.4.2 fully closed |
| WS-10 | CI/CD gate MVP | Policy engine, PR checks, deploy gates, override auditing | WS-03, WS-05, WS-06, WS-09 | `[in-progress]` |
| WS-11 | PR generation MVP | Fix proposal pipeline, audit trail, provider integration | WS-03, WS-05, WS-06, WS-09 | `[done]` |
| WS-12 | API and dashboard v1 | Findings, SBOM, trust scores, attack surface, reports | WS-03, WS-04, WS-06, WS-07, WS-08 | `[in-progress]` |
| WS-13 | Prompt and supply-chain intelligence | Prompt Injection Shield, trust scoring, maintainer signal analysis | WS-03, WS-04, WS-05 | `[done]` |
| WS-14 | Graph and autonomy systems | Blast radius graph, Red/Blue loop, memory controller, learning loop | WS-03, WS-04, WS-09 | `[done]` — Phase 1–4 complete + Multi-Cloud Blast Radius (spec §3.12): cloud SDK→resource inference, 27 tests, `cloudBlastRadiusSnapshots` schema, fire-and-forget wiring in blastRadiusIntel, `RepositoryCloudBlastRadiusPanel` |
| WS-15 | Compliance and hardening | Regulatory drift, privacy controls, deployment modes, auditability, observability | WS-03, WS-04, WS-12 | `[done]` — Splunk/Elastic SIEM push (spec §4.6.5) ✅; Jenkins closes final CI/CD provider gap (spec §4.6.2) ✅; HF model provenance enrichment (spec §3.11.2) ✅; Teams + Opsgenie alerts (spec §4.6.3) ✅ |
| WS-16 | Tier 3 Threat Intelligence | CISA KEV catalog integration, Telegram Bot webhook, dark-web signal ingestion | WS-07, WS-15 | `[done]` — CISA KEV daily sync (parseCisaKevResponse + matchCisaKevToCveList + exploitAvailable patching, 26 tests); Telegram Bot webhook (parseTelegramPost + scoreMessageThreatLevel + credential/ransomware detection, 24 tests); `tier3Intel.ts` "use node" action module; `cisaKevSnapshots` + `tier3ThreatSignals` schema tables; `POST /webhooks/telegram`, `GET /api/threat-intel/cisa-kev`, `POST /api/threat-intel/cisa-kev/sync` HTTP routes; `ThreatIntelPanel` dashboard component; 996/996 tests |
| WS-17 | Analyst Feedback Loop | `false_positive`/`ignored` finding statuses, analyst triage event log, confidence multiplier feedback into learning loop | WS-14, WS-15, WS-16 | `[done]` — `findingTriageEvents` schema table (3 indexes); `lib/findingTriage.ts` pure library (triageActionToStatus, computeTriageSummary, analystFpRate, analystConfidenceMultiplier; 25 tests); `findingTriage.ts` Convex entrypoints (applyTriageAction + convenience mutations + getTriageHistory + getFalsePositiveSummary + loadTriageEventsForLearningLoop); one-line feedback loop in `learningProfileIntel.ts` (FP status → unexploitable override); `PATCH/GET /api/findings/triage` HTTP routes; `RepositoryLearningPanel` FP count pill; 1021/1021 tests |
| WS-18 | SLA Enforcement Engine | Time-to-remediate accountability with per-severity deadlines, breach detection cron, MTTR, Slack notifications, compliance report | WS-17 | `[done]` — `lib/slaPolicy.ts` pure library (DEFAULT_SLA_POLICY: critical 24h/high 72h/medium 7d/low 30d, assessSlaFinding, computeSlaSummary with MTTR; 32 tests); `slaBreachEvents` schema table (append-once dedup by_finding); `slaIntel.ts` Convex entrypoints (checkSlaBreaches internalMutation + checkAllSlaBreaches fan-out + getSlaStatusForRepository + getSlaBreachHistory + getSlaComplianceReport + getSlaStatusBySlug + triggerSlaCheckForRepository); `sendSlaBreachNotification` internalAction in slack.ts; hourly `sla breach check` cron; `GET /api/sla/status` HTTP endpoint; `RepositorySlaPanel` dashboard component; 1053/1053 tests |
| WS-19 | Risk Acceptance Lifecycle | Governed accept-risk workflow with temporary/permanent levels, auto-expiry cron, Slack notifications, HTTP API | WS-17, WS-18 | `[done]` — `lib/riskAcceptance.ts` pure library (isExpired/isExpiringSoon/formatExpiryText/computeExpiresAt/computeAcceptanceSummary; 26 tests); `riskAcceptances` schema table (4 indexes including by_status for O(log n) expiry scan); `riskAcceptanceIntel.ts` Convex entrypoints (createRiskAcceptance + revokeRiskAcceptance mutations + checkExpiredAcceptances internalMutation + 5 queries); `sendAcceptanceExpiryNotification` internalAction in slack.ts; hourly expiry check cron; `POST/DELETE /api/findings/risk-accept` + `GET /api/findings/risk-acceptances` HTTP routes; `RepositoryRiskAcceptancePanel` dashboard component; 1079/1079 tests |
| WS-20 | Cross-Repository Impact Detection | Lateral package exposure analysis: when a supply-chain disclosure matches a repo, scan all other tenant repos for the same package | WS-07, WS-19 | `[done]` — `lib/crossRepoImpact.ts` pure library (normalizeForCrossRepo, matchesPackage with unknown-ecosystem bypass, assessRepositoryImpact, computeCrossRepoImpact + buildCrossRepoSummary; 26 tests); `crossRepoImpactEvents` schema table (upsert by tenantId+normalizedPackageName; impacts[] array; 3 indexes); `crossRepoIntel.ts` Convex entrypoints (computeAndStoreCrossRepoImpact internalMutation with per-repo SBOM snapshot loading; getCrossRepoImpact + getTenantCrossRepoSummary + getCrossRepoImpactBySlug + getTenantCrossRepoSummaryBySlug queries); fire-and-forget wiring as final step in events.ingestCanonicalDisclosure; `GET /api/findings/cross-repo-impact` HTTP endpoint; `TenantCrossRepoPanel` global dashboard component; 1105/1105 tests |
| WS-21 | Automated Remediation Priority Queue | Composite P0/P1/P2/P3 scoring that merges SLA status, exploit availability, blast radius, validation outcome, and severity into a ranked queue per repository | WS-18, WS-19, WS-20 | `[done]` — `lib/remediationPriority.ts` pure library (additive signal model: SLA breach +40/approaching +25/exploit +20/validation +15/blast radius +5-+15/severity +2-+10; clamped 100; tier thresholds P0≥70/P1≥45/P2≥20/P3<20; computeRemediationScore, classifyPriorityTier, prioritizeRemediationQueue with createdAt tiebreak, computeQueueSummary; 30 tests); `remediationQueueIntel.ts` Convex entrypoints (getRemediationQueueForRepository + getRemediationQueueBySlug; active risk acceptances excluded; blast radius Map pattern; no new schema table); `GET /api/remediation/queue` HTTP endpoint (API-key-guarded, limit capped at 100); `RepositoryRemediationQueuePanel` per-repository dashboard component (tier count pills, avg score, top-5 findings with rationale); 1135/1135 tests |
| WS-22 | Finding Severity Escalation Engine | Makes severity dynamic by synthesizing blast radius, CISA KEV exploit availability, cross-repo spread, and SLA breach into monotone automatic severity upgrades with a full audit trail | WS-14, WS-16, WS-18, WS-20 | `[done]` — `lib/escalationPolicy.ts` pure library (5 triggers: exploit_available→critical, blast_radius_critical→high, cross_repo_spread→high, blast_radius_high→medium, sla_breach→medium; escalateSeverityForTrigger +1/run monotone; assessEscalation max-wins multi-trigger, informational/critical boundaries; DEFAULT_ESCALATION_POLICY; 66 tests); `severityEscalationEvents` schema table (append-only audit; 3 indexes by_finding/by_repository/by_tenant); `escalationIntel.ts` Convex entrypoints (checkAndEscalateFinding internalMutation atomically patches severity + inserts audit row; runEscalationSweepForRepository internalMutation fan-out; runAllEscalationSweeps cron target; getEscalationHistoryForFinding + getEscalationSummaryForRepository + getEscalationSummaryBySlug queries); 12th webhook event type `finding.severity_escalated` + `FindingSeverityEscalatedData`; every-4h escalation sweep cron; `GET /api/findings/escalations` HTTP endpoint; reactive fire-and-forget wiring in blastRadiusIntel + crossRepoIntel; `RepositoryEscalationPanel` dashboard component; 1201/1201 tests |
| WS-23 | Autonomous Remediation Dispatch | Closes the WS-21 → WS-11 loop: selects P0 findings from the priority queue, applies a concurrency-capped opt-in policy, and schedules `proposeFix` automatically | WS-21, WS-22, WS-11 | `[done]`
| WS-24 | VS Code Extension | Phase 4 IDE Integration — inline findings diagnostics, manifest CodeLens, security posture status bar, findings webview panel | WS-12 | `[done]` — 58 tests; `findPackageLine()` per-format resolver implemented for 8 manifest formats |
| WS-25 | Agentic Workflow Security Scanner | Phase 4 — detects orchestration-level vulnerabilities across LangChain/CrewAI/AutoGen/LlamaIndex/Vercel AI SDK: unbounded loops, privilege escalation, data exfiltration chains, memory poisoning, insecure inter-agent communication | WS-13 (llm_callchain) | `[done]` — 33 Python tests; `agentic_workflow.py` analyzer; `POST /analyze/agentic-workflows`; Convex `agenticWorkflowIntel.ts`; `agenticWorkflowScans` schema; `RepositoryAgenticWorkflowPanel`; agent-core v0.3.0 | — `lib/autoRemediation.ts` pure library (AutoRemediationPolicy, DEFAULT policy enabled=false, isTierEligible, selectRemediationCandidates with 5 skip reasons; 30 tests); `autoRemediationRuns` schema table (append-only audit; 2 indexes); `autoRemediationIntel.ts` Convex entrypoints (triggerAutoRemediationForRepository internalMutation: builds priority queue inline, loads open/draft PR count for concurrency cap, selectRemediationCandidates, schedules proposeFix fire-and-forget, inserts audit row; runAllAutoRemediationDispatches cron target; getAutoRemediationHistoryForRepository + getAutoRemediationSummaryBySlug queries); daily `auto remediation dispatch` cron (02:00 UTC); `GET /api/remediation/auto-runs` HTTP endpoint; `RepositoryAutoRemediationPanel` dashboard component; `autoRemediationIntel` + `lib/autoRemediation` registered in api.d.ts; 1231/1231 tests |
| WS-27 | Vendor Trust & OAuth Risk | SaaS/OAuth vendor risk management — stable vendor inventory per tenant, append-only risk snapshot audit log, 7-rule scoring model (0–100 score, 4 risk levels, 4 recommendations), daily sweep cron | WS-14, WS-16 | `[done]`
| WS-28 | Gamification Layer | Sprint leaderboard for attack surface reduction — repository ranking by score delta over configurable window, engineer leaderboard by fix-PR attribution, gold/silver/bronze badges, Monday cron, dashboard panel | WS-14 Phase 3, WS-11 | `[done]`
| WS-29 | Production Traffic Anomaly Detection | Agent-less HTTP access log monitoring — 6 detection passes (error spike, path enumeration, suspicious user-agent, latency outliers, injection attempts, request flood), combinatorial scoring, 4-level classification, finding creation for anomalous events, HTTP ingest endpoint | WS-13, WS-09 | `[done]`
| WS-30 | Hardcoded Credential & Secret Detection | Push-event secret scanning — 19 regex detectors across 10 credential families + Shannon entropy analysis; placeholder filtering; test-file hint; `secretScanResults` schema table; fire-and-forget on every push; per-repo dashboard panel | WS-05 | `[done]`
| WS-31 | Dependency License Compliance Engine | SPDX classification (6 categories) + 200+ package static DB across npm/pypi/cargo/go + DEFAULT_COMMERCIAL_POLICY weighted scoring (blocked×20 + warn×5); `licenseComplianceSnapshots` schema table; fire-and-forget on every SBOM ingest; per-repo dashboard panel | WS-06 | `[done]` — 44 pure-lib tests; `getLatestLicenseCompliance` / `getLicenseComplianceSummaryByTenant` queries; `RepositoryLicenseCompliancePanel`; 1495/1495 tests |
| WS-32 | SBOM Quality & Completeness Scoring | 5-axis quality grade per SBOM snapshot: completeness (25%), version-pinning (25%), license-resolution (20%), freshness (15%), layer-coverage (15%); grades excellent/good/fair/poor; `sbomQualitySnapshots` schema table; fire-and-forget on every SBOM ingest; per-repo sub-score dashboard panel | WS-06, WS-31 | `[done]` — 42 pure-lib tests; `getSbomQualityForRepository` / `getSbomQualitySummaryByTenant` queries; `RepositorySbomQualityPanel`; 1537 tests |
| WS-33 | IaC Security Scanner | Static misconfiguration detection for Terraform (6 rules), Kubernetes (6 rules), Dockerfile (5 rules), Docker Compose (3 rules); 20 total rules across critical/high/medium severity; `iacScanResults` schema table; fire-and-forget on push when IaC files detected; per-repo dashboard panel | WS-05 | `[done]` — 46 pure-lib tests; `getLatestIacScan` / `getIacScanSummaryByTenant` queries; `RepositoryIacScanPanel`; 1583 tests |
| WS-34 | EPSS Score Integration | FIRST.org exploitation-probability enrichment: daily sync patches `epssScore`+`epssPercentile` onto breach disclosures; `classifyEpssRisk` (critical ≥0.5/high ≥0.2/medium ≥0.05/low); `epssSnapshots` schema table; `GET /api/threat-intel/epss` + `POST /api/threat-intel/epss/sync`; daily 04:00 UTC cron; `EpssThreatIntelPanel` dashboard | WS-16 (CISA KEV) | `[done]` — 42 pure-lib tests; `syncEpssScores` internalAction; `getLatestEpssSnapshot` / `getEpssEnrichedDisclosures` queries; 1625 tests |
| WS-35 | CI/CD Pipeline Security Scanner | Static misconfiguration detection for GitHub Actions (6 rules), GitLab CI (4 rules), CircleCI (3 rules), Bitbucket Pipelines (2 rules), and 2 cross-platform rules; 17 total rules; `cicdScanResults` schema table; fire-and-forget on push when CI/CD files detected; per-repo dashboard panel | WS-05 | `[done]` — 48 pure-lib tests; `getLatestCicdScan` / `getCicdScanSummaryByTenant` queries; `RepositoryCicdScanPanel`; 1673 tests |
| WS-36 | Community Rule/Fingerprint Marketplace | Spec §10 Phase 4 network-effect moat — operators submit fingerprint patterns and detection rules, community votes/reports, approved contributions flow into the platform's detection library; `communityContributions` + `contributionVotes` schema tables; 4 HTTP routes; `CommunityMarketplacePanel` dashboard | WS-13 (semantic fingerprinting) | `[done]` — 34 pure-lib tests (`computeContributionScore`, `isApprovalEligible`, `deriveStatus`, `validateContribution`, `summarizeMarketplaceStats`, `rankContributions`); `submitContribution`/`voteOnContribution`/`reportContribution`/`approveContribution`/`rejectContribution`/`listContributions`/`getMarketplaceStats`/`getTopContributors`/`getApprovedByVulnClass` entrypoints; 1734 tests |
| WS-37 | Cryptography Weakness Detector | Static detection of broken/deprecated crypto algorithms in source code on every push — 16 rules across 8 languages (Python, JS/TS, Java, Go, Ruby, C#, PHP, Rust): broken hashes (MD5/SHA-1), broken ciphers (DES/RC4/Blowfish), insecure modes (ECB/CBC-no-MAC), weak randomness, weak password hashing, short RSA keys, cert-verify disabled, deprecated TLS, null cipher, base64 as encryption, hardcoded zero IV; `cryptoWeaknessResults` schema table; fire-and-forget on push; per-repo dashboard panel | WS-05 | `[done]` — 61 pure-lib tests; `recordCryptoWeaknessScan`/`getLatestCryptoWeaknessScan`/`getCryptoWeaknessSummaryByTenant` entrypoints; `GET /api/crypto/weaknesses`; `RepositoryCryptoWeaknessPanel`; 1734 tests |
| WS-38 | Dependency & Runtime End-of-Life (EOL) Detection | Static EOL database (33 entries: Node.js 10/12/14/16, Python 2.7/3.6/3.7/3.8, Ruby 2.7/3.0, PHP 7.4/8.0/8.1, .NET 5/6/7, Django 1/2.2/3.2, Flask 1, Rails 5/6.0/6.1, Spring Boot 2.5/2.6, Log4j 1, Angular 12/13/14, request/node-uuid/core-js-2/jQuery-1/2); segment-safe version-prefix matching; 90-day near-EOL window; three-way null split (supported vs unknown); `eolDetectionResults` schema table; fire-and-forget on SBOM ingest; per-repo dashboard panel | WS-06 | `[done]` — 46 pure-lib tests; `recordEolScan`/`triggerEolScanForRepository`/`getLatestEolScan`/`getEolScanHistory`/`getEolSummaryByTenant` entrypoints; `GET /api/eol/scan`; `RepositoryEolPanel`; 1780/1780 tests |
| WS-39 | Open-Source Package Abandonment Detector | Static abandonment database (27 entries across 5 reason types): supply-chain-compromised critical (event-stream, flatmap-stream, ua-parser-js v0), archived/unmaintained high (request, phantomjs, phantomjs-prebuilt, pycrypto, cryptiles v3, therubyracer), officially-deprecated medium (tslint, node-sass, bower, karma, babel-polyfill, sklearn, nose), superseded/unmaintained low (node-uuid, popper.js, left-pad, istanbul, jshint, coffee-script, distribute, mock v1/v2, commons-logging 1.1, grunt v0, core-js v2); `abandonmentScanResults` schema table; fire-and-forget on SBOM ingest after EOL scan; per-repo dashboard panel | WS-06, WS-38 | `[done]` — 46 pure-lib tests; `recordAbandonmentScan`/`triggerAbandonmentScanForRepository`/`getLatestAbandonmentScan`/`getAbandonmentScanHistory`/`getAbandonmentSummaryByTenant` entrypoints; `GET /api/abandonment/scan`; `RepositoryAbandonmentPanel`; 1826/1826 tests |
| WS-40 | SBOM Attestation | Pure-JS SHA-256 (FIPS 180-4, Uint32Array arithmetic, verified against openssl/Python/Node.js) content fingerprinting + tenant-scoped attestation hash; status lifecycle unverified→valid/tampered; `sbomAttestationRecords` schema table (3 indexes: by_snapshot, by_repository_and_attested_at, by_tenant_and_attested_at); fire-and-forget on SBOM ingest after abandonment scan; per-repo dashboard panel showing content hash + attestation hash + status | WS-06, WS-39 | `[done]` — 35 pure-lib tests; `recordSbomAttestation`/`verifySnapshotAttestation` internalMutations + `triggerAttestationForRepository`/`getLatestAttestation`/`getAttestationBySnapshotId`/`getAttestationSummaryByTenant` entrypoints; `GET /api/sbom/attestation`; `RepositoryAttestationPanel`; 1861/1861 tests |
| WS-41 | Dependency Confusion Attack Detector | Purely static heuristic detection of Alex Birsan-style attacks: three signals (extreme_version ≥9000→critical, high_version_unknown_scope ≥99→high/medium, high_version_internal_name ≥49→high/medium); `KNOWN_PUBLIC_NPM_SCOPES` allowlist (~60 scopes); `INTERNAL_NAME_PATTERNS` regex set; ecosystem-lowercased deduplication; `confusionAttackScanResults` schema table (2 indexes); fire-and-forget on SBOM ingest after attestation; `GET /api/sbom/confusion-scan`; per-repo dashboard panel (risk pill + suspicious count + top 5 findings, suppressed when clean) | WS-06, WS-40 | `[done]` — 54 pure-lib tests; `recordConfusionScan`/`triggerConfusionScanForRepository`/`getLatestConfusionScan`/`getConfusionScanHistory`/`getConfusionSummaryByTenant` entrypoints; `RepositoryConfusionScanPanel`; 0 TS errors |
| WS-42 | Malicious Package Detection | Three-layer purely static heuristic detector: (1) `known_malicious` curated DB of 15 confirmed typosquats/backdoors (crossenv, discordio, mongose, electorn, etc.) → critical/high; (2) `typosquat_near_popular` Levenshtein ≤1 from top-100 npm packages with length guard → high (unscoped npm only); (3) `suspicious_name_pattern` beyond-edit-distance signals (l/1 and o/0 homoglyphs, numeric-suffix-on-popular `lodash2`, scope squatting `@npm/lodash`) → medium; findings sorted critical-first; `maliciousPackageScanResults` schema table (2 indexes); fire-and-forget after confusion scan; `GET /api/sbom/malicious-scan`; per-repo dashboard panel (signal-annotated findings with "Resembles:" link, suppressed when clean) | WS-06, WS-41 | `[done]` — 55 pure-lib tests; `recordMaliciousScan`/`triggerMaliciousScanForRepository`/`getLatestMaliciousScan`/`getMaliciousScanHistory`/`getMaliciousSummaryByTenant` entrypoints; `RepositoryMaliciousScanPanel`; 0 TS errors |
| WS-44 | Supply Chain Posture Score | Synthesis aggregator that re-runs all five supply-chain sub-scanners (EOL, Abandonment, CVE, Malicious Package, Dependency Confusion) inline plus reads SBOM attestation status to produce a single 0–100 composite score and A–F grade per repository; penalty model: CVE (cap –50), Malicious (cap –50), Confusion (cap –40), Abandonment (cap –35), EOL (cap –25), Attestation (–20 tampered / –5 unverified); risk level escalates to critical on any critical-severity finding regardless of score; `supplyChainPostureScores` schema table (2 indexes); fire-and-forget last step in sbom.ts chain; `GET /api/sbom/supply-chain-posture`; per-repo dashboard panel with prominent A–F grade letter + score/100 + per-category penalty breakdown | WS-38, WS-39, WS-40, WS-41, WS-42, WS-43 | `[done]` — 67 pure-lib tests; `recordSupplyChainPosture`/`triggerSupplyChainPostureForRepository`/`getLatestSupplyChainPosture`/`getSupplyChainPostureHistory`/`getSupplyChainPostureSummaryByTenant` entrypoints; `RepositorySupplyChainPosturePanel`; 0 TS errors |
| WS-45 | Container Image Security Analyzer | Detects security risks in container base images declared in SBOM components (ecosystems 'docker', 'container', 'oci'); static `CONTAINER_IMAGE_DATABASE` (~45 entries: ubuntu/debian/alpine/node/python/postgres/php/mysql/redis/nginx + deprecated codenames); signals: `eol_base_image` (critical), `near_eol` (high, 90-day window), `outdated_base` (medium/low), `no_version_tag` (medium — 'latest'/'*'/empty), `deprecated_image` (high/critical); O(1) `_IMAGE_INDEX` Map at module load; `matchVersionPrefix` handles `-`/`.`/`_` separators; registry prefix stripping (e.g. `docker.io/library/ubuntu` → `ubuntu`); `containerImageScanResults` schema table (2 indexes); fire-and-forget final step in sbom.ts chain after posture score; `GET /api/sbom/container-image-scan`; per-repo dashboard panel (risk pills + images-scanned count + top-5 findings with signal label/EOL date/recommended upgrade) | WS-44 | `[done]` — 74 pure-lib tests; `recordContainerImageScan`/`triggerContainerImageScanForRepository`/`getLatestContainerImageScan`/`getContainerImageScanHistory`/`getContainerImageScanSummaryByTenant` entrypoints; `RepositoryContainerImagePanel`; 0 TS errors |
| WS-46 | Compliance Attestation Report Generator | Maps outputs from all 12 supply-chain/security scanners to per-framework regulatory compliance status across SOC 2 Type II (7 controls: CC6.1/CC6.6/CC6.7/CC7.1/CC7.2/CC8.1/CC9.2), GDPR (3: Art.25/Art.32/Art.33-34), PCI-DSS 4.0 (4: Req.6.2/6.3/6.5/11.3), HIPAA (4: §164.312 a(1)/a(2)(iv)/c(1)/e(2)(ii)), NIS2 (4: Art.21(2)(e)/(h)/(i)/(j)); GAP_PENALTIES: critical=20/high=12/medium=6/low=3 subtracted from 100 per framework; status: `non_compliant` if criticalGaps>0, `at_risk` if highGaps>0 OR score<75, else `compliant`; reads latest persisted scanner results (not inline re-run) with 5s `runAfter` delay; `complianceAttestationResults` schema table (2 indexes); `GET /api/compliance/attestation`; per-repo dashboard panel (per-framework status pills with score, self-hides when all compliant+no gaps) | WS-45 | `[done]` — 73 pure-lib tests; `recordComplianceAttestation`/`triggerComplianceAttestationForRepository`/`getLatestComplianceAttestation`/`getComplianceAttestationHistory`/`getComplianceAttestationSummaryByTenant` entrypoints; `RepositoryCompliancePanel`; 0 TS errors |
| WS-47 | Compliance Gap Remediation Planner | Maps WS-46 control gaps to concrete, ordered remediation playbooks; 22-entry `REMEDIATION_CATALOG` one per control; 9 root causes (`CONTROL_ROOT_CAUSE` map) deduplicate `estimatedTotalDays` (same underlying weakness counted once across frameworks, max days); actions sorted critical→high→medium→low; `automatableActions` counts actions with ≥1 automatable step; `complianceRemediationSnapshots` schema table (2 indexes); reads latest WS-46 attestation with 7s `runAfter` delay; `GET /api/compliance/remediation-plan`; per-repo dashboard panel (effort pills + automatable count + top-5 prioritised actions with severity/effort pills, self-hides when totalActions === 0) | WS-46 | `[done]` — 79 pure-lib tests; `recordComplianceRemediationPlan`/`triggerComplianceRemediationPlanForRepository`/`getLatestComplianceRemediationPlan`/`getComplianceRemediationPlanHistory`/`getComplianceRemediationPlanSummaryByTenant` entrypoints; `RepositoryRemediationPlanPanel`; 0 TS errors |
| WS-48 | License Compliance & Risk Scanner | SPDX-identifier-based license risk classification complementing WS-31's policy engine; 70-entry `LICENSE_DATABASE` (permissive/weak_copyleft/strong_copyleft/proprietary); `SPDX_ALIASES` map handles colloquial variants (GPLv2, MIT License, Apache 2.0); `resolveCompoundLicense` splits "MIT AND GPL-3.0" / "MIT OR GPL-3.0" expressions conservatively (worst wins); riskLevel: strong_copyleft → critical, weak_copyleft/proprietary → high, unknown → medium, deprecated_spdx → low; `licenseComplianceScanResults` schema table (2 indexes); `runAfter(0)` fire-and-forget on SBOM ingest; `GET /api/sbom/license-scan`; per-repo dashboard panel (overallRisk pill + critical/high/unknown count pills + top-5 per-package findings with spdxId + licenseType label, self-hides when all none) | WS-31, WS-06 | `[done]` — 91 pure-lib tests; `recordLicenseComplianceScan`/`triggerLicenseComplianceScanForRepository`/`getLatestLicenseComplianceScan`/`getLicenseComplianceScanHistory`/`getLicenseComplianceScanSummaryByTenant` entrypoints; `RepositoryLicenseScanPanel`; 0 TS errors |
| WS-50 | Dependency Update Recommendation Engine | Reads CVE (WS-43), EOL (WS-38), and abandonment (WS-39) scanner findings to produce deduplicated, prioritised update recommendations; deduplication by `ecosystem::name` key (case-insensitive); `parseSemver` handles v-prefix/pre-release/Maven suffixes/2-segment versions; `classifyEffort`: patch (same major+minor) / minor (same major) / major (different major) / replacement (different package); `isMajorBump` → `breakingChangeRisk`; urgency inherits highest severity from contributing findings; sorting: urgency desc → effort asc → alphabetical; `isReplacementPackage` heuristic (digit-only = version, letters = package name); `ABANDONMENT_LABEL` for human-readable reason labels; `dependencyUpdateRecommendations` schema table (recommendations[] capped at 50, 2 indexes); `runAfter(11000)` fire-and-forget completing cascade 0s→5s→7s→9s→11s; `GET /api/sbom/update-recommendations`; per-repo dashboard panel (urgency/effort pills + top-5 with version→target + migrate-to indicator, self-hides when totalRecommendations === 0) | WS-43, WS-38, WS-39 | `[done]` — 68 pure-lib tests; `recordDependencyUpdateRecommendations`/`triggerDependencyUpdatesForRepository`/`getLatestDependencyUpdateRecommendations`/`getDependencyUpdateHistory`/`getDependencyUpdateSummaryByTenant` entrypoints; `RepositoryDependencyUpdatePanel`; 0 TS errors |
| WS-49 | Repository Security Health Score | Master capstone synthesis layer reading latest results from 8+ scanner tables (WS-44 Supply Chain, WS-43 CVE, WS-38 EOL, WS-39 Abandonment, WS-37 Crypto, WS-30 Secrets, WS-33 IaC, WS-35 CI/CD, WS-46 Compliance, WS-45 Container, WS-48 License, WS-32 SBOM Quality); 7 weighted categories (supply_chain 25%, vulnerability_management 20%, code_security 15%, compliance 15%, container_security 10%, license_risk 10%, sbom_quality 5%); penalty-based per-category scoring with capped deductions (e.g. CVE crit -20 cap -60, secret crit -20 cap -60); `scoreToGrade` A≥90/B≥75/C≥60/D≥40/F<40; `detectTrend` improving/declining/stable/new (±5 threshold); topRisks max 5 from lowest-scoring categories; `repositoryHealthScoreResults` schema table (2 indexes); `runAfter(9000)` fire-and-forget after WS-47 at 7s; `GET /api/repository/health-score`; per-repo dashboard panel (4xl score + grade pill + trend + 7 mini category bars + top risk bullets, self-hides when grade A + stable/new + no risks) | WS-44, WS-43, WS-38, WS-39, WS-37, WS-30, WS-33, WS-35, WS-46, WS-45, WS-48, WS-32 | `[done]` — 105 pure-lib tests; `recordRepositoryHealthScore`/`triggerRepositoryHealthScoreForRepository`/`getLatestRepositoryHealthScore`/`getRepositoryHealthScoreHistory`/`getRepositoryHealthScoreSummaryByTenant` entrypoints; `RepositoryHealthScorePanel`; 0 TS errors |
| WS-43 | Known CVE Version Range Scanner | Offline static CVE database (30 entries across npm/maven/pypi) with minimum-safe-version thresholds; O(1) pre-indexed Map keyed by `${ecosystem}:${name}`; `parseVersionTuple` strips v-prefix, Maven `.RELEASE`/`.SNAPSHOT`, PyPI pre-release suffixes; `isVersionVulnerable` returns boolean\|null; `cvssToRiskLevel` maps to NVD standard thresholds (≥9.0 critical/≥7.0 high/≥4.0 medium/else low); notable CVEs: Log4Shell CVE-2021-44228 (CVSS 10.0), Spring4Shell CVE-2022-22965 (9.8), Text4Shell CVE-2022-42889 (9.8), vm2 sandbox escape CVE-2023-29017 (9.8), werkzeug RCE CVE-2024-34069 (9.8); `cveVersionScanResults` schema table (2 indexes); fire-and-forget after malicious scan; `GET /api/sbom/cve-scan`; per-repo dashboard panel (CVE ID + CVSS score pills + v→fix annotation, suppressed when clean) | WS-06, WS-42 | `[done]` — 50 pure-lib tests; `recordCveScan`/`triggerCveScanForRepository`/`getLatestCveScan`/`getCveScanHistory`/`getCveSummaryByTenant` entrypoints; `RepositoryCveScanPanel`; 0 TS errors |

| WS-52 | Security Debt Velocity Tracker | Point-in-time security debt snapshot per repository: net velocity (new/day − resolved/day), SLA overdue counts, projected clearance days, 0–100 debt score, improving/stable/degrading/critical trend; fire-and-forget on every finding creation; `GET /api/security/debt`; per-repo dashboard panel | WS-18, WS-20 | `[done]` — 35 pure-lib tests; `classifyTrend`/`computeDebtScore`/`computeSecurityDebtVelocity` pure functions; `securityDebtSnapshots` schema table (2 indexes); `securityDebtIntel.ts` (6 entrypoints: computeAndStoreSecurityDebt internalMutation + triggerSecurityDebtForRepository + getLatestSecurityDebt + getLatestSecurityDebtBySlug + getSecurityDebtHistory + getSecurityDebtSummaryByTenant); `RepositorySecurityDebtPanel`; 2650/2650 tests |
| WS-53 | GitHub Branch Protection Analyzer | Static 8-rule evaluation of default-branch protection configuration: NO_BRANCH_PROTECTION (critical), NO_REQUIRED_REVIEWS/FORCE_PUSH_ALLOWED (high), NO_REQUIRED_STATUS_CHECKS/STALE_REVIEWS_NOT_DISMISSED/NO_CODEOWNERS (medium), ADMIN_BYPASS_ALLOWED/DELETIONS_ALLOWED (low); 0–100 riskScore with per-severity caps; internalAction fetches real data from GitHub Branch Protection API (404 → enabled=false) + CODEOWNERS endpoint; simulated fallback when GITHUB_TOKEN absent; `branchProtectionResults` schema table (dataSource field: github_api|simulated; 2 indexes); fire-and-forget in events.ts after security debt; `GET /api/repository/branch-protection`; per-repo dashboard panel (risk pills + top-5 findings with severity + recommendation) | WS-05 | `[done]` — 42 pure-lib tests; `recordBranchProtectionScan`/`checkAndStoreBranchProtection`/`triggerBranchProtectionCheck`/`getLatestBranchProtectionScan`/`getLatestBranchProtectionBySlug`/`getBranchProtectionScanHistory`/`getBranchProtectionSummaryByTenant` entrypoints; `RepositoryBranchProtectionPanel`; 2692/2692 tests |

## Milestones

| Milestone | Outcome | Target Workstreams | Status |
|---|---|---|---|
| M0 | Repo is ready for implementation | WS-01, WS-02 | `[in-progress]` |
| M1 | Core platform can receive events and persist workflow state | WS-03, WS-04 | `[in-progress]` |
| M2 | Phase 1 scanning pipeline works end-to-end on a single repo | WS-05, WS-06, WS-07, WS-08, WS-09 | `[not-started]` |
| M3 | Sentinel can block or annotate CI and open auditable PRs | WS-10, WS-11 | `[done]` |

| M4 | Dashboard and public API expose customer-facing value | WS-12 | `[done]` |
| M5 | Phase 2 intelligence features begin landing incrementally | WS-13, WS-14, WS-15 | `[in-progress]` |

## Phase Mapping To The Spec

| Spec phase | What we should actually build |
|---|---|
| Phase 1: Foundation | WS-01 through WS-12 |
| Phase 2: Intelligence | WS-13 plus early WS-14 |
| Phase 3: Autonomy | Mature WS-14 plus enterprise deployment pieces in WS-15 |
| Phase 4: Ecosystem | Extensions, SDKs, marketplace, IDE integrations |

## Immediate Risks

- Convex is now initialized locally and deployment-backed code generation works, but the GitHub webhook path still needs a real repository secret and delivery test to be exercised end-to-end.
- The SBOM pipeline now includes local multi-ecosystem parsing across npm, pnpm, Yarn, Bun, Python, Go, Rust, and container-native sources, plus a bridge command that can import worker output into Convex and dashboard drilldowns with drift and vulnerable-inventory summaries, but it still needs a live Convex deployment and GitHub delivery path to be exercised end-to-end.
- Breach intake now supports GitHub Security Advisory and OSV normalization, version-aware SBOM matching, live ID-based advisory imports, the GitHub webhook path in code, scheduled or bulk sync in code, and aggregator-style sync-run visibility in the dashboard, but it still needs the first live sync run plus broader Tier 2 and Tier 3 feed coverage beyond the current authoritative sources.
- Semantic fingerprinting now has a path-aware MVP in the Convex control plane with candidate findings, workflow-stage advancement, and dashboard visibility, but it still lacks the full embedding, tree-sitter, and vector-search stack from the long-term spec.
- Exploit validation now has a local-first MVP that records validation runs, classifies findings, advances validation workflow stages, and exposes recent evidence in the dashboard, but it still lacks the real sandbox lifecycle, artifact capture, and post-fix replay loop from the long-term design.
- The local machine does not currently have Go installed, so the Go services are architectural boundaries rather than verified runtimes today.
- The long-term spec still requires specialized stores for vectors, graphs, and sandbox artifacts; we should keep the MVP control-plane contracts clean so those additions remain incremental.
- WS-11 (PR generation) is fully complete including real manifest file editing for pypi/npm ecosystems with a transparent tracking-file fallback; M3 is now done.
- WS-12 (API and dashboard v1) is complete: operator findings API (list/get/stats), SBOM export (CycloneDX 1.5), trust score Strategy B (direct-weighted mean), repository drilldown, HTTP auth guard via `SENTINEL_API_KEY`. M4 is done.
- WS-13 (Prompt and supply-chain intelligence) is fully complete: 18-pattern prompt injection scanner, supply chain typosquat detection + dependency risk signals, Convex entrypoints (`scanContent`, `scanContentByRef`, `recentScans`, `supplyChainAnalysis`), `promptInjectionScans` schema table, fire-and-forget ingestion wiring into GitHub push (commit messages) and breach sync (advisory summary+description) paths, and `RepositoryIntelligencePanel` dashboard component surfacing supply chain risk + injection scan history per repository card. All checks green (125 tests, tsc, biome, build).
- WS-14 Phase 1 (Blast Radius Causality Graph foundation) is complete: `convex/lib/blastRadius.ts` pure computation library (computeBlastRadius → BlastRadiusResult with spec formula), `convex/lib/blastRadius.test.ts` (19 tests: no-components, direct-dep, transitive chain, multi-service blast, risk tier boundaries, score cap), `blastRadiusSnapshots` schema table (with by_finding + by_repository_and_computed_at indexes), `convex/blastRadiusIntel.ts` (computeAndStoreBlastRadius internalMutation, getBlastRadius + blastRadiusSummaryForRepository public queries), fire-and-forget scheduling via `ctx.scheduler.runAfter` wired into `ingestCanonicalDisclosure`, `FindingBlastRadiusPanel` + `RepositoryBlastRadiusSummary` dashboard components. All checks green (144 tests, tsc, biome, build).
- WS-14 Phase 2 (Memory Controller + Red/Blue Loop MVP) is complete: pure libs (memoryController 17 tests + redBlueSimulator 14 tests), schema tables (agentMemorySnapshots + redBlueRounds), Convex entrypoints (agentMemory.ts + redBlueIntel.ts), fire-and-forget wiring, dashboard panels. All checks green (175 tests).
- WS-14 Phase 3 (Attack Surface Reduction Agent MVP) is complete: pure lib attackSurface.ts (29 tests, score formula max=100), attackSurfaceSnapshots schema table, attackSurfaceIntel.ts Convex entrypoints, fire-and-forget wiring in events.ts, RepositoryAttackSurfacePanel with CSS sparkline. All checks green (204 tests).
- WS-14 Phase 4 (Red Agent Finding Escalation) is complete: pure lib redAgentEscalator.ts (37 tests; parses package/depth exploit chain formats, builds FindingCandidate[]), redAgentEscalation.ts Convex entrypoints (escalateRedAgentFindings internalMutation with dedupeKey idempotency guard, getRedAgentFindingCount query), fire-and-forget on red_wins in redBlueIntel.ts, escalation count pill in AdversarialRoundPanel. All checks green (241 tests, tsc, biome, build).
- Session 2 additions: classify_outcome() implemented (16 tests); Tier 1 breach feeds complete (NVD+npm+PyPI+RustSec+GoVuln, 5 adapters+5 sync actions, 14 new tests); Slack integration (Block Kit alerts, weekly digest, wired into finding.validated+gate.blocked); GitLab webhook (Push Hook+MR Hook, shared-secret auth, idempotent push event mutation). All checks green (453 Convex tests, 63 Python tests).
- Session 7 additions: Tier 3 breach feeds (paste site+HIBP+dark web scaffold, 14 new tests → 569 Convex total); Zero-day anomaly detection (zeroDayAnomaly.ts: centroid drift, adaptive thresholds, 21 tests, wired into analyzeCodeChange); GitHub Enterprise Server support (githubClient.ts: GHES_BASE_URL override, advisory sync updated); MSSP white-label API (mssp.ts: 5 HTTP routes, tenant CRUD, cross-tenant dashboard, MSSP_API_KEY guard); LLM call chain detection (llm_callchain.py: Python AST + JS regex, 14 frameworks, 4-tier input classification, POST /analyze/llm-callchains, 12 new Python tests → 24 agent-core total). All checks green: 569 Convex, 22 sbom-ingest, 63 sandbox, 24 agent-core Python tests.
- Session 6 additions: Tier 2 breach feeds (GitHub Issues, HackerOne, oss-security RSS, Packet Storm RSS; 4 normalizers, 4 sync actions, 17 new tests → 534 total Convex); Real attack surface analysis (import_graph.py AST analyzer for JS/TS+Python, unused/test-only/single-use detection, agent-core upgraded to v0.2.0 with POST /analyze/attack-surface, 12 new Python tests); Azure DevOps webhook (Basic auth, git.push+git.pullrequest.merged, SCM coverage complete: GitHub/GitLab/Bitbucket/ADO all done). All checks green: 534 Convex, 22 sbom-ingest, 63 sandbox, 12 agent-core Python tests.
- Session 5 additions: Blue Agent (blueAgent.ts 22 tests + blueAgentIntel.ts + detectionRuleSnapshots schema + GET /api/detection-rules endpoint; nginx/ModSec/Splunk/Elastic/Sentinel/LogRegex formats); Post-Fix Validation Loop (postFixValidation.ts: PR merge → sandbox re-validation → resolve or regression Slack alert; GitHub webhook now handles pull_request:merged); Linear GraphQL integration (createLinearIssue/completeLinearIssue/getLinearTicketsForRepository); Bitbucket Cloud webhook (HMAC-SHA256, repo:push, pullrequest:fulfilled → post-fix). All checks green: 517 Convex tests (+22), tsc, biome, build.
- Session 4 additions: GitHub Actions native CI action (apps/github-action/, 14 tests; action.yml + sentinel-api.ts + check-run.ts + index.ts + example workflow); Supply Chain Social Monitor Phase 1 (real GitHub API: archived/abandoned/new-account/contributor-velocity/build-script/release signals, supplyChainAnalyses schema table); Jira Cloud integration (ADF body builder, createJiraIssue/resolveJiraIssue/getJiraTickets); NuGet (csproj+packages.lock.json) + Composer (composer.lock+composer.json) SBOM parsers completing all 8 ecosystems (6 new Python tests → 22 total sbom-ingest). All checks green: 495 Convex tests, 22 Python sbom-ingest, 63 Python sandbox, 14 GitHub Action.
- Session 3 additions: Semantic Fingerprinting Phase 1 with real OpenAI embeddings (codeEmbedding.ts + 52-pattern vulnerabilityPatternLibrary, analyzeCodeChange action, 2 schema tables, 30 tests, RepositorySemanticFingerprintPanel); SPDX 2.3 export (spdx.ts pure builder, 20 tests, exportSnapshotAsSpdx, format=spdx endpoint); Maven/Gradle/Ruby SBOM parsers (pom.xml namespace-aware, build.gradle Groovy+KTS, Gemfile.lock/Gemfile, 11 new Python tests → 16 total sbom-ingest tests); PagerDuty integration (Events API v2, trigger/resolve, severity threshold, wired into honeypot triggers). All checks green: 495 Convex tests (+42), 16 Python sbom-ingest tests (+11), 63 Python sandbox tests.
- WS-09 Phase 1 (Exploit-First Sandbox Validation — real HTTP execution) is in progress: Python `services/sandbox-manager` service (FastAPI + httpx; HttpProbeModule, InjectionModule, AuthBypassModule, CVE pattern registry, executor, PoC generator; 45 tests); `sandboxEnvironments` Convex table; `sandboxValidation.ts` Convex action layer (trigger, persist, fail, summary queries); fire-and-forget wiring in events.ts; `/api/sandbox/environment` + `/api/sandbox/summary` HTTP endpoints; `RepositorySandboxPanel` dashboard component. ⚠️ `classify_outcome()` left as user contribution. All checks green (442 tests, tsc, biome, build).

- WS-45 (Container Image Security Analyzer) is complete: `convex/lib/containerImageSecurity.ts` pure library (~45-entry static DB, `checkContainerImage`, `computeContainerImageReport`, `matchVersionPrefix`, `isUnpinnedTag`, `_IMAGE_INDEX` O(1) Map); `containerImageSecurity.test.ts` (74 tests, 74/74 ✓); `containerImageScanResults` schema table (2 indexes); `containerImageIntel.ts` (5 entrypoints: `recordContainerImageScan`/`triggerContainerImageScanForRepository`/`getLatestContainerImageScan`/`getContainerImageScanHistory`/`getContainerImageScanSummaryByTenant`); fire-and-forget in sbom.ts chain (final step after posture score); `GET /api/sbom/container-image-scan` HTTP route; `RepositoryContainerImagePanel` dashboard component; `containerImageIntel` + `lib/containerImageSecurity` registered in `api.d.ts`; 0 TS errors.

- WS-46 (Compliance Attestation Report Generator) is complete: `convex/lib/complianceAttestationReport.ts` pure library (5-framework GAP_PENALTIES model, 22 control checks across SOC2/GDPR/PCI-DSS/HIPAA/NIS2, `computeComplianceAttestation`); `complianceAttestationReport.test.ts` (73 tests, 73/73 ✓); `complianceAttestationResults` schema table (2 indexes); `complianceAttestationIntel.ts` (5 entrypoints: `recordComplianceAttestation` reads 12 scanner tables via `Promise.all`/`triggerComplianceAttestationForRepository`/`getLatestComplianceAttestation`/`getComplianceAttestationHistory`/`getComplianceAttestationSummaryByTenant`); fire-and-forget in sbom.ts chain with `runAfter(5000)` delay after container image scan; `GET /api/compliance/attestation` HTTP route; `RepositoryCompliancePanel` dashboard component (per-framework status+score pills, self-hides when all compliant+no gaps); `complianceAttestationIntel` + `lib/complianceAttestationReport` registered in `api.d.ts`; 0 TS errors.

- WS-47 (Compliance Gap Remediation Planner) is complete: `convex/lib/complianceRemediationPlanner.ts` pure library (22-entry `REMEDIATION_CATALOG` one entry per WS-46 control, 9-root-cause `CONTROL_ROOT_CAUSE` map for `estimatedTotalDays` deduplication, `computeRemediationPlan`); `complianceRemediationPlanner.test.ts` (79 tests, 79/79 ✓); `complianceRemediationSnapshots` schema table (2 indexes); `complianceRemediationIntel.ts` (5 entrypoints: `recordComplianceRemediationPlan` reads latest WS-46 attestation → flattens controlGaps → `computeRemediationPlan` / `triggerComplianceRemediationPlanForRepository` / `getLatestComplianceRemediationPlan` / `getComplianceRemediationPlanHistory` lean / `getComplianceRemediationPlanSummaryByTenant`); fire-and-forget in sbom.ts chain with `runAfter(7000)` (after WS-46 at 5 s); `GET /api/compliance/remediation-plan` HTTP route; `RepositoryRemediationPlanPanel` dashboard component (effort pills, automatable count, top-5 action list, self-hides when totalActions === 0); `complianceRemediationIntel` + `lib/complianceRemediationPlanner` registered in `api.d.ts`; 0 TS errors.

- WS-48 (License Compliance & Risk Scanner) is complete: `convex/lib/licenseComplianceScanner.ts` pure library (70-entry `LICENSE_DATABASE` covering permissive/weak_copyleft/strong_copyleft/proprietary; `SPDX_ALIASES` map for colloquial variants like "GPLv2"/"MIT License"; `resolveCompoundLicense` splits "MIT AND GPL-3.0" expressions conservatively; `computeLicenseCompliance`); `licenseComplianceScanner.test.ts` (91 tests, 91/91 ✓); `licenseComplianceScanResults` schema table (2 indexes); `licenseScanIntel.ts` (5 entrypoints: `recordLicenseComplianceScan` reads latest SBOM snapshot components → `computeLicenseCompliance` → insert → prune to 30/repo / `triggerLicenseComplianceScanForRepository` / `getLatestLicenseComplianceScan` / `getLicenseComplianceScanHistory` lean (strips descriptions) / `getLicenseComplianceScanSummaryByTenant`); fire-and-forget with `runAfter(0)` in sbom.ts after WS-31 license compliance scan; `GET /api/sbom/license-scan` HTTP route; `RepositoryLicenseScanPanel` dashboard component (overallRisk + critical/high/unknown count pills + top-5 per-package findings with spdxId + licenseType label, self-hides when all risks are 'none'); `licenseScanIntel` + `lib/licenseComplianceScanner` registered in `api.d.ts`; 0 TS errors.

## Working Rules

- Every new major feature must map back to a workstream and milestone.
- `CURRENT_CONTEXT.md` must be updated at the end of every meaningful run.
- `TODO.md` should only contain the near-term build queue.
- If scope changes, update `IMPLEMENTATION_SPLIT.md` first, then reflect the status change here.
