#compdef cyberzen
_arguments '1:command:(auth agents billing dashboard tenants link unlink findings repos scan scans sbom drift gates attack trust threat compliance reports sla remediation security crypto repository traffic webhooks siem honeypot sandbox marketplace mssp memory integrations jobs status system)'

function _cyberzen() {
  _arguments '1:command:(auth agents billing dashboard tenants link unlink findings repos scan scans sbom drift gates attack trust threat compliance reports sla remediation security crypto repository traffic webhooks siem honeypot sandbox marketplace mssp memory integrations jobs status system)'
}
compdef _cyberzen cyberzen
