{
  "name": "sentinel-github-action",
  "version": "1.0.0",
  "description": "Sentinel Security Gate — GitHub Actions native CI integration",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node --format cjs",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.5.0",
    "bun-types": "^1.1.0"
  }
}
